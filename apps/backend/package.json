{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start:api": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "generate": "bunx prisma generate",
    "migrate": "bun prisma migrate dev",
    "start": "bun run build && bun run src/index.ts",
    "build": "bun prisma migrate deploy && bun run generate",
    "vercel-build": "prisma migrate deploy && prisma generate",
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "tsx": "^4.20.6"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "prisma": "^6.16.2",
    "ts-node": "^10.9.2",
    "zod": "^4.1.11"
  }
}
